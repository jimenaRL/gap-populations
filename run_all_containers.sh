country=romania; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=slovakia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=luxembourg; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=hungary; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=iceland; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=malta; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=cyprus; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=slovenia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=latvia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=lithuania; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=estonia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=portugal; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=switzerland; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=serbia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=austria; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=newzealand; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=denmark; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=norway; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=belgium; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=ecuador; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=czechia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=sweden; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=peru; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=uruguay; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=greece; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=finland; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=israel; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=ireland; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=netherlands; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=poland; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=australia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=nigeria; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=germany; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=chile; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=canada; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=france; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=venezuela; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=italy; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=colombia; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=argentina; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=eu; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=spain; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=japan; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=mexico; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=southafrica; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=uk; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=turkey; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=brazil; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")
country=us; cwd=$(pwd); docker run -d --name=gap_${country} -v ${cwd}/${country}.db:${cwd}/${country}.db -v ${cwd}/wip:${cwd}/wip gap bash -c "source anonymized_db.sh ${country} && source gap.sh ${country}")